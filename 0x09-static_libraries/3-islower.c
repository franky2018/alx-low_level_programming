#include "main.h"

/**
 * _islower - Check for lowercase characters
 * @c: the character to check
 *
 * Return: 1 if program is lowercase, or else 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
